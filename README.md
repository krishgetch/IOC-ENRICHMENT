## Bulk Lookup (Node.js)

Run with npm:

1. Create `.env` file next to `server.js` with:

```
VT_API_KEY=your_virustotal_api_key
ABUSE_API_KEY=your_abuseipdb_api_key
PORT=8080
```

2. Install and start:

```
npm install
npm run start
```

Open `http://localhost:8080`.


